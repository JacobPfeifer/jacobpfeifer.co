{% set navPages = collections.all | eleventyNavigation  %}

{% macro renderNavListItem(entry,rel) -%}

  {% if entry.url == page.url or entry.title == eleventyNavigation.parent or entry.title == eleventyComputed.key%}
    {% set highlight = 'font-medium text-gray-900 focus:border focus:border-gray-700' %}
    {% set current = 'aria-current="page"' %}
  {% else %}
    {% set highlight = 'text-gray-900 focus:border focus:border-gray-700' %}
    {% set current = '' %}
  {% endif %}
 
  <a class="
    w-max 
    mx-4 my-2
    px-2
    py-1
    block
    focus:text-gray-900 
    bg-transparent 
    dark:bg-transparent
    mt-0 
    m-0
    text-lg
    shadow-brand
    hover:shadow-encompass
    transform
    transition
    duration-200
    ease-in-out
    focus:outline-none focus:shadow-encompass focus:ring-2 focus:ring-gray-700 {{ highlight }} 
    box" 
    href="{{ entry.url | url }}">{{ entry.title }}
  </a>
{%- endmacro %}

<div class="md:bg-white dark:bg-gray-900 p-0 flex w-full justify-end">
  <div class="flex justify-start items-center flex-row ">
    <nav class="flex flex-row my-4 justify-end flex-grow ">
    {%- for entry in navPages %}
    {{ renderNavListItem(entry,'child') }}
    {%- endfor -%}
    </nav>
  </div>
</div>
